# API Configuration
# For development, the proxy is used automatically (see vite.config.ts)
# For production, set the actual backend URL
VITE_API_BASE_URL=https://tfshrms.cloud/email/
VITE_UPLOAD_ENDPOINT=/email_send_import/upload
